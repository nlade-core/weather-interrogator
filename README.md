# weather-interrogator

Short-term weather forecast for Edinburgh, pulled live from [Open-Meteo](https://open-meteo.com/) (no API key needed — safe for a static site). A chart navigates the raw hourly data into something readable; the raw JSON is still there, collapsed, for anyone who wants to check the tool's working.

Live: https://nlade-core.github.io/weather-interrogator/

## Why

Weather APIs return a wall of hourly arrays. Most questions people actually have ("do I need an umbrella," "is it worth a BBQ this weekend") only need a couple of those numbers, weighed together. This is a testbed for going from raw data to a useful answer.

## MVP (current)

- Fetch is pinned to `models=ukmo_uk_deterministic_2km` explicitly rather than `best_match` — same grid cell, same values for every field that's genuinely UKV (confirmed identical to best_match's), but a field UKV can't produce (like `precipitation_probability`, which needs an ensemble UKV doesn't have) comes back null instead of best_match silently substituting a mismatched ~27km source and us displaying it as if it were local. `precipitation_probability` is deliberately not fetched at all
- Chart shows temperature at 15-minute resolution (a visibly smoother, more precise line than hourly) with predicted rainfall as a full-height wash behind it, Apple Weather-style — bar **height is predicted amount in mm directly** (ceiling ~3mm/15min, solidly "heavy"), not probability. Both series share one time axis so they line up correctly; raw JSON available collapsed
- Precip wash is coloured by type (blue rain/drizzle, pale cyan snow, violet thunderstorm/hail) from `weathercode` — confirmed against the docs that weathercode is an *instant* reading, so it's a plain "most recent known reading" lookup
- Layout, top to bottom: date (once) and hour labels at the top — the conventional position, not buried at the bottom — then the main plot with condition icons riding the temperature line's curve (Yr-style; genuine 15-min `weathercode` data, moved from `hourly` to `minutely_15` once confirmed it's derived per-timestep from `cloud_cover` etc. rather than hourly-native), one icon per 15-min point. Dual Y-axes: temperature on the left with "nice" round-number ticks, rainfall on the right (fixed 0/1/2/3mm — that's what bar *height* encodes; type lives in colour, not position). The old inline "16°" point labels were removed — redundant now that there's both an axis for general scale and a working hover tooltip for exact values. A change-triggered icon-thinning approach (only refresh at most every 30min) was tried and reverted — back to full 15-min density
- Wind: arrow (direction) + speed number at the bottom, one pair per 15-min point — tried merging into a single size-scaled arrow (bigger = stronger, no separate number) but reverted; the explicit number reads more clearly than inferring speed from size alone. Exact value also available via hover, same pattern as everything else on the chart
- Custom hover (dashed guide line, snapped highlight, floating tooltip) instead of native title-tooltips — same interaction pattern as Plotly/Yr, hand-rolled with no charting library
- "Ask" is wired to Chrome's built-in Gemini Nano (Prompt API) — on-device, no server. Edinburgh's 15-min temperature, 15-min rain amount (mm), 15-min wind (speed+direction), hourly wind gusts, and hourly conditions (type) for the rest of today are all baked into one persistent session's system prompt at creation time, so follow-up questions share context instead of starting fresh each time. Gusts get the same preceding-hour-max shift precipitation probability needed. No rain-probability figure is included — deliberately, since the only one available wasn't locally reliable
- Each question runs as two turns in that session rather than one: first a short "what does this need, is it covered" unpacking step, then the answer — small on-device models do better at combining two data series into a judgment when made to state what's needed before answering, rather than jumping straight there
- The model is pre-warmed at page load when it's already downloaded; a "downloadable" state waits for your first Ask click to trigger anything, with real download-progress percentages shown rather than a page that just looks stuck
- Chat-style log under Ask: your question right-aligned, the model's reasoning step and answer left-aligned and visually distinct, status/error lines centered — every state transition (preparing, downloading, understanding, answering, error) visible instead of failing silently
- No build step, no dependencies — plain HTML/CSS/JS

## Roadmap

- [ ] **Paused here (2026-09-06) — five feature ideas raised together in one session, to unpack one at a time next time, no particular order:**
  1. **Mobile-friendly vis.** *Scaling* already works (SVG viewBox, confirmed rendering cleanly at a 390px viewport) but that's not the same thing as *mobile-friendly* — real open questions, not yet thought through: hover is currently the only way to get an exact value off the chart, and there's no hover on touch; icon density at 15-min resolution is already tight at typical desktop width, a phone makes it tighter; the Ask input bar sits pinned to the bottom of its own panel, not the viewport, so what an on-screen keyboard does to that layout hasn't been checked at all. Needs a think about the touch interaction model (tap-to-reveal instead of hover? thin the icons only on narrow viewports?) before any code changes.
  2. **Data from further time periods, visualised effectively.** Same idea as the existing "multi-day view" item further down this list — resurfaced because it's now a real want, not a someday. Genuine design unpack needed: a second day of 15-min-resolution detail is a completely different chart from a 7-day daily-summary strip, and they'd probably want different treatments rather than one chart trying to do both. The Ask side isn't free of this either — the system prompt currently states outright "no other days," and `CONTEXT_HOURS` as a fixed rolling window doesn't obviously generalise to "this weekend" or "next Tuesday" without rethinking how the staged pipeline's category/lookup step would pick a time range as well as a data category.
  3. **Change location.** Same idea as the existing location-picker item further down. Mechanically simple on its face (swap the hardcoded `EDINBURGH` constant for a chosen coordinate) but touches Edinburgh-specific copy across the whole app (page subtitle, footer, system prompt wording, this README), and needs a geocoding approach that doesn't require a paid API key or account, matching the no-signup/no-key philosophy the project has kept to everywhere else so far (Open-Meteo needs none; Nominatim/OSM's free geocoding API is the obvious first candidate, not yet checked).
  4. ~~Indicate night-time on the chart.~~ **Done (2026-09-06).** `is_day` added to the `minutely_15` fetch (confirmed it flips at real 15-min resolution, not just a repeated hourly value — checked against an actual sunrise: 06:15 still 0, 06:30 already 1). Shaded background band for `is_day === 0` stretches, painted first so it sits behind the precip wash and both icon rows; edges placed at the midpoint between the two samples either side of each transition rather than snapping to a sample point, since the real sunrise/sunset falls somewhere between two readings. Coloured a distinct dark blue (`#0a1330`) rather than plain black, since the panel background is already near-black and a black overlay barely read as different. Verified against a real sunset-to-sunrise window (19:13 window start, sunset 20:00, sunrise 06:30 next day): screenshot showed what looked like the band running to the chart's right edge with no gap, but querying the actual rendered rect geometry confirmed it correctly stops ~50px (of 688px plot width) before the edge, matching the real ~43-minute daylight gap between sunrise and the window's 07:13 end — a zoomed screenshot at that boundary then confirmed it directly. Worth remembering: a compressed full-width screenshot is not a reliable way to verify a ~40px detail; querying the actual DOM/attribute values (or zooming in) is.
  5. **Relative importance of variables — at what point does wind, temperature, sunlight, or rain become the primary consideration, and how should the chart reflect that?** The open-ended one, no concrete design yet. Directly continues an earlier discussion this session about wind becoming decision-dominant past roughly 25-30km/h (now mph) and temperature mostly only mattering at Edinburgh's cold/hot extremes rather than across its normal day-to-day range — `apparent_temperature`'s gap from the actual reading was one concrete, shippable instance of exactly this kind of signal (now on the chart, continuously plotted). The bigger unresolved question is a general mechanism for it: some way to surface "here's what actually matters about today's forecast" rather than presenting every variable with equal visual weight regardless of whether it's the one thing that would actually change someone's plans that day. Sunlight/UV isn't even fetched yet (see `uv_index` in the remaining-fields bullet below) so this would need that groundwork too. Needs real unpacking as a design problem, not a quick feature to bolt on.

- [x] **Built (2026-09-06): recent-past trailing strip on the chart.** ~~Scoped, not built~~ — Grew out of a broader question — is a rigid rolling 12h-forward window the right one, and how should future/past/historical data be shown at all? Conclusion: those are three separate features, not one bigger chart. Multi-day future is item 2 above (a different chart shape entirely, daily summary not 15-min detail). True historical/archival browsing (how the model did last week) is a different feature again, deliberately not scoped here — no clear use case yet beyond curiosity, and it doesn't serve the app's day-to-day purpose the way the other two do. This item is the middle one: a few hours of **recent past** context (was it just raining, is temp rising or falling) alongside the existing forward window, since "is this arriving or leaving" is a real gap the current forward-only view can't answer, and it pairs naturally with the webcam feature's whole premise of checking forecast against reality.

  **Feasibility, verified, not assumed:** Open-Meteo's `past_days` fetch parameter extends both `minutely_15` and `hourly` arrays backward by full calendar days, at the *same* resolution as the forward data (confirmed via `past_days=1`: `minutely_15` came back 192 points spanning two full days instead of one, still genuinely 15-min throughout, `hourly` likewise). No separate past-data endpoint or resolution downgrade — same one-fetch architecture as everywhere else in the app. Note `past_days` grants whole extra calendar days from midnight, not "N hours before now" — getting e.g. "past 3 hours" means fetching `past_days=1` and then slicing with the same `nextHoursIndices`-style windowing already used for the forward side, just walking backward from `now` instead of forward.

  **How it was actually built, resolving the open design questions:**
  - **`PAST_HOURS = 3`**, kept deliberately shorter than the 12h forward window so it reads as context, not a second panel. A separate constant from `CONTEXT_HOURS`, and a new `windowIndices(times, now, pastHours, futureHours)` function kept entirely separate from `nextHoursIndices` — the Ask/LLM lookup path still calls `nextHoursIndices` directly and stays forward-only, exactly as scoped; it has no way to accidentally start seeing past data.
  - **Now marker**: a new always-visible vertical line + "now" label (`chart-now-marker`/`chart-now-label`), distinct from the existing hover guide which only appears on interaction. **Past de-emphasis**: `chart-past` class at 45% opacity on condition/wind icons directly, but precip bars needed a different approach — `.precip-bar` already sets its own opacity per type (rain/snow/storm), so adding a second opacity class to the *same* `<rect>` would just override it via the CSS cascade (the exact presentation-attribute-vs-class precedence bug this project hit once before with the wind icons), losing the type distinction. Fixed by wrapping past bars in their own `<g class="chart-past">` instead — group opacity compounds with a child's own opacity rather than colliding with it.
  - **`xForTime`/`spanMs` reworked** to offset from `windowStart` (`now - PAST_HOURS`) rather than `now` — touched, as expected, since every other position on the chart derives from it.
  - **Night-band and axis code needed zero changes**, confirming the earlier prediction — neither assumed `now` was the left edge.
  - **Real bug caught during testing, not before shipping:** `past_days=1` (added for this) also shifts the *daily* summary's array — `daily.time[0]` becomes yesterday instead of today whenever `past_days` is set, which broke the hardcoded index-0-is-today assumption in `renderDailySummary` (below) and was only caught because testing showed three day-cards instead of two. Fixed by locating "today" via matching `data.current.time`'s date against `daily.time`, then slicing from there — robust to `past_days` regardless of its value, not just patched for `past_days=1` specifically.
  - **Deliberately still out of scope:** feeding this into the Ask pipeline (every other data addition this session shipped chart-first, Ask-category second — this should follow the same order) and true observed/station data (Gogarbank NOAA ISD or the Met Office DataHub, both already dead ends or heavier lifts noted elsewhere — `past_days` model data is a good-enough proxy for recent context, not a claim of ground truth).
  - Verified with real data (hover tooltip correctly says "was 17:30" for a past point vs. "at 06:15" for a future one) and at a 390px mobile viewport.

- [x] **Built (2026-09-06): 2-day summary ("Today"/"Tomorrow") for the "further time periods" want above.** Not the 7-day view that phrase might imply — checked first rather than assumed: `models=ukmo_uk_deterministic_2km` with `forecast_days=7` came back with real `daily` data for exactly 2 days, then `null` for days 3 onward. Building a real week would mean mixing in a lower-resolution global model for the missing days, the same resolution-mismatch problem `precipitation_probability` was dropped over — so this ships an honest 2-day card strip (icon, high/low, rain total) instead, with the UKV-range limitation stated directly in the panel's own copy rather than left implicit. New "Coming days" panel, its own `daily` fetch block. Ask/system-prompt integration and the full 7-day design question (different chart shape, different Ask category/time-range handling) both remain open, exactly as scoped in item 2 above.

- [x] Confirmed working end-to-end in real desktop Chrome — user tested manually after the chat rework (two-turn reasoning, chat-bubble log, precise download/preparing status)
- [ ] Revisit hourly vs. 15-minutely context comparison — dropped for now in favour of a single persistent 15-minutely session, worth re-adding once the core chat flow is confirmed solid
- [x] Root cause of the ~90-100% probability / ~0.0mm pattern, now *confirmed* rather than inferred: `precipitation_probability` comes from a **~27km generic global ensemble** (stated outright in Open-Meteo's own docs), while temperature/amount/weathercode come from the UK-specific **2km UKV** deterministic model. Different resolution, different source — not a bug, not the model contradicting itself. Checked MOGREPS-UK's actual 3 members directly for a live case: all three agreed on ~0mm while `best_match` showed 99% — strong evidence the coarse global ensemble is the mismatched one, not the local data
- [x] Decided against keeping the mismatched probability field around even as a labelled secondary — dropped it entirely. Pinned the fetch to `models=ukmo_uk_deterministic_2km` explicitly (confirmed identical values to best_match for every field that matters; only difference is the gap is now null instead of silently backfilled), reworked the wash to run on predicted mm directly, fixed the one spot in the system prompt that referenced probability
- [ ] **Paused, revisit later:** computing our own locally-coherent probability/expected-rainfall to fill that gap properly, rather than leaving it blank. Two feasible paths, both confirmed working: (a) `ukmo_uk_ensemble_2km` (MOGREPS-UK, 3 members, UK-specific 2km) or (b) a self-built multi-model ensemble (5 independent national models — UKMO/ECMWF/DWD/GFS/Météo-France — in one `models=` request, more genuine spread than (a)). (b) is the better option if/when we do this
- [x] Wind (speed, direction, gusts) added to both chart and model context — see MVP above
- [x] `apparent_temperature` added — chart, current-card, tooltip, and Ask category. Initially shipped with the chart line itself gated on divergence (only drawing where it crossed `APPARENT_TEMP_GAP = 2`°C from actual), but that made the line appear/disappear as it crossed the threshold, which read worse than just seeing the two lines' gap directly — **changed to plot continuously the full window length**, same as the actual-temperature line; the current-card and tooltip's "(feels X°)" text keep the divergence gating since text clutter and a chart line are different concerns. Axis bounds now always stretch to fit both lines. Verified with real data (a real ~1.4°C gap after dark) and a fake `LanguageModel` double confirming a "wind chill" phrased question correctly pulls both `apparent_temperature` and `wind` into the lookup.
- [x] Wind speed added as a chart line with a genuine third axis (right side, further out than the precip mm axis, coloured to match the wind line so the two visibly belong together). First attempt was a quick self-scaled demo (no real axis, just stretched to fill the plot height) shown for feedback before building — correctly called out as misleading (couldn't read an actual km/h value off it, and it crossed the temp/feels-like lines in ways that looked meaningful but weren't). Rebuilt properly: real min/max floored/ceiled to steps of 5, `niceAxisTicks` (renamed from `niceTemperatureTicks` since it's now shared by both axes) generating genuine labelled ticks. Verified with real data and at a narrow mobile viewport (SVG scales via viewBox, no clipping). The arrow+speed row at the bottom is unchanged — direction still isn't something a line can show.
- [ ] Remaining fields from the confirmed UKV variable list (see Data notes), in priority order and why: `uv_index` (cheap, a threshold badge not a chart element, despite seasonal value) → `cloud_cover` (adds a continuous "how overcast" signal `weathercode` doesn't) → `cape` (refines the existing storm classification rather than covering a gap) → `is_day` (priority raised 2026-09-06 — see the night-time-indication item in the paused block at the top of this Roadmap; no longer "cosmetic, matters later," matters for the existing single-window chart too). `relative_humidity_2m`/`dew_point_2m` demoted to LLM-context-only — `apparent_temperature` already folds humidity in, so a separate line would mostly restate it. `visibility`, `pressure_msl`/`surface_pressure`, `wind_speed_80m`, `soil_temperature_0cm`, `et0_fao_evapotranspiration`, `vapour_pressure_deficit` — genuinely out of scope, don't change an outdoor-activity decision
- [ ] Idea, paused: once rain probability/expected-rainfall is properly reintroduced (see the paused ensemble item above), present it as a distribution rather than a single number — e.g. a raindrop-shaped, blue-coloured visual conveying spread/uncertainty rather than one point estimate. Not started
- [ ] Multi-day view for weekend-scale questions — revisited and unpacked further in the paused block at the top of this Roadmap (2026-09-06)
- [ ] Consider a location picker beyond Edinburgh — revisited and unpacked further in the paused block at the top of this Roadmap (2026-09-06)
- [x] **Chart rebuilt as icons-only, no plotted lines at all — shipped for observation, not a settled design.** Both the wind-line-with-real-axis and the apparent-temperature line (from the two commits before this one) removed; replaced with two icon strings sharing one fixed 0-25 scale (temperature in °C, wind in mph): condition icons (already existed, unchanged) ride the temperature height, a rotated arrow rides the wind-speed height (direction via rotation is the icon's own "extra info", same relationship condition icons have with weather type). Real implications worth weighing before calling this final:
  - **`apparent_temperature` no longer has any chart presence at all** — dropped rather than made a third icon string, since the request was specifically for two. It's still in the current-card, tooltip, and Ask category — only the chart line is gone.
  - **Fixed 0-25 scale clamps anything outside that range** — fine for Edinburgh's typical spread, but a hard frost (below 0°C) or a heatwave (above 25°C) would visually pin to the axis edge rather than showing the real value. Not built to handle extremes, deliberately, for this version.
  - Also switched wind speed to **mph throughout** (chart, current-card, tooltip, Ask categories) via Open-Meteo's `wind_speed_unit=mph` fetch parameter — UK convention (Met Office forecasts, broadcast weather) is mph, not km/h, so this wasn't chart-specific, it touched every wind display in the app.
  - `niceTemperatureTicks` renamed to `niceAxisTicks` in the previous commit is now dead code (fixed 0-25 ticks replaced it) and was removed.
  - Verified with real data and at a 390px mobile viewport, no console errors either way.
- [x] ~~Pivot from chart cosmetics to the LLM side.~~ **Done.** Two things shipped together since both the chart and Ask needed the same fix:
  - **Fixed window: both the chart and Ask now cover a rolling next-12-hours span** (`CONTEXT_HOURS`), not "rest of today" — behaves the same at 8am or 11pm, and can cross midnight (chart date labels handle that: a second date label appears right at the day boundary if the window spans one).
  - **Staged Ask pipeline, replacing the old single-dump-then-ask approach.** The old design baked the *entire* forecast into the session's system prompt at creation — exactly the failure mode a small on-device model can't handle (it gets lost combining several raw numeric series into a judgment). New flow, all visible in the log so a wrong answer can be traced to the exact stage it went wrong: (1) restate the user's actual goal, (2) pick which of a **fixed** category vocabulary (temperature/rain/wind/gusts/conditions) that goal needs — closed vocabulary rather than free text, so the next step is a genuine deterministic lookup rather than more fuzzy parsing, with a logged fallback to a default set if parsing the model's pick fails, (3) plain JS pulls only those categories for the 12h window — no model involved, (4) answer using only that filtered data. System prompt now only describes what's available, not the numbers themselves.
  - Verified structurally end-to-end (Playwright): download-progress flow, log ordering, chart's 12h window and midnight-crossing date labels all confirmed with a fabricated late-evening fixture. **Not yet verified: real answer quality with an actual downloaded Gemini Nano** — Chromium's built-in Prompt API test double only echoes prompts back, it doesn't run a real model, so answer *quality* (does the staging actually produce better judgments than the old one-shot version) still needs checking in real desktop Chrome.
  - Still pending, unrelated to this change: `uv_index` isn't in the category vocabulary yet (`apparent_temperature` now is — see below) — would close more of the remaining gap for "is it worth a BBQ"-style comfort questions.
  - **Multi-turn now supported** — the session persists across questions like before, but only the *first* question in a session runs the full staged pipeline; every follow-up just calls `session.prompt(question)` directly against the context already built up (previous goal/lookup/answer). Deliberately simple for now.
  - **Idea, not started: a lighter pipeline specifically for follow-ups.** Skipping straight to a raw answer works while the follow-up is close to the first question, but won't hold up once someone asks something that needs different data than what got looked up the first time (e.g. first question about rain, follow-up about wind) — the model would be answering from data it was never actually given. Likely shape: something with the same intent-then-lookup spirit as the first-question pipeline, but lighter — e.g. skip re-deriving the goal from scratch and just check whether the existing lookup already covers the new question, doing a fresh (or additional) lookup only if it doesn't.

- [ ] Shipped as v1 earlier: **live computer vision via the on-device model** — "Look outside" panel, `getUserMedia` + canvas frame capture + a separate `LanguageModel` session with `expectedInputs: [{ type: 'image' }]`, following `chrome-chat`'s working content-part shape. Standalone for now — just describes the sky from one frame, no comparison to the forecast yet.

- [ ] **Webcam feature, next steps:**
  1. Integrate it as an actual data source into the product — feed its read of the sky into the same context the Ask model uses (e.g. does the frame agree with what UKV predicted for right now), rather than it living as a separate disconnected panel.
  2. **Current real friction: this only works with a laptop's own webcam, and carrying a laptop around to point out a window is impractical.** Right fix is getting the phone to sync with this properly (companion capture, or just phone-as-browser once there's a reason to make the site itself mobile-friendly) — parking this until that sync work exists rather than working around it with something disposable.

## Data notes

- Open-Meteo has no map/radar/tile imagery — point-location JSON only. For Edinburgh, the default `best_match` model resolves to the **UK Met Office's own UKV model (2km resolution)**, the best available option for this location, not a generic global grid.
- Historical **observed** data goes back to 1940 (reanalysis-based) via the Historical Weather API.
- Historical **forecast** data (what the model predicted, not what happened) is also available, separately: the **Previous Model Runs API** (`previous-runs-api.open-meteo.com`) returns past forecasts at fixed lead times (1-7 days ahead) via `_previous_dayN` suffixed variables — this is what a real probability-calibration check for Edinburgh would be built on, comparing predicted vs. observed rather than trusting either general model reputation or reasoned guesses. Caveat found while trying this: `precipitation_probability_previous_dayN` is always null — probability specifically isn't archived by this API, only deterministic fields (temp, amount, etc.) are
- **Confirmed UKV (2km) variable list**, checked by requesting `models=ukmo_uk_deterministic_2km` explicitly and seeing what's actually non-null (not assumed): `temperature_2m`, `apparent_temperature`, `dew_point_2m`, `relative_humidity_2m`, `vapour_pressure_deficit`, `precipitation`, `rain`, `snowfall`, `weather_code`, `cloud_cover` (+low/mid/high), `visibility`, `uv_index`, `pressure_msl`, `surface_pressure`, `wind_speed_10m`, `wind_speed_80m`, `wind_direction_10m`, `wind_gusts_10m`, `is_day`, `cape`, `freezing_level_height`, `soil_temperature_0cm`, `et0_fao_evapotranspiration` — all genuinely at 15-min resolution (checked consecutive values actually vary, not just the hourly figure repeated 4x). **Not** from UKV, comes from elsewhere under `best_match`: `precipitation_probability`, `showers`, `snow_depth`, `evapotranspiration`, `uv_index_clear_sky`, `soil_moisture_0_to_1cm`.
- The `_2m`/`_10m`/`_80m` suffixes are height-above-ground (temperature measured at 2m, wind at 10m and separately at 80m — standard WMO diagnostic heights), unrelated to the model's 2km horizontal grid spacing — easy to conflate, they're different axes entirely.
- Open-Meteo doesn't interpolate between grid points for a requested coordinate — it **selects one grid cell**, by default the on-land cell whose elevation (90m DEM) best matches the request, then statistically adjusts for any remaining elevation gap. Confirmed empirically: querying a fine mesh of ~170 points across ~10km around Edinburgh resolved to 28 distinct real grid cells with genuinely varying elevation (17-159m) and temperature (15.7-17.3°C) between neighbours — interpolating across those would wash out real terrain-driven local variation, not just smooth noise.
- `wind_speed_10m` is "instant" but behaves nothing like a real spot observation — checked empirically (same Gogarbank week, modeled vs. NOAA-observed hour-to-hour changes): the model's mean/stdev hour-to-hour jump was 1.16/1.13 km/h vs. the real station's 2.34/2.08 km/h — roughly **half the variance**. Read: a 2km/hourly-timestep model can't resolve real turbulence, so its "instant" wind is really the smoothed resolved-scale field with turbulence parameterized away, not a genuine point sample the way a real anemometer reading is — which is exactly why gusts exist as a separate variable. **Open question, deliberately not resolved:** does the same variance-reduction logic apply to *gusts*? Gusts are already an explicit extreme/max (not an average), so the "forecast converges toward the expectation" reasoning that explains temperature/sustained-wind's lower variance may not transfer cleanly. Flagged for later, not answered.
- Gogarbank's *archived* (NOAA ISD) cadence is confirmed hourly, on the hour — but that's what the archive retains, not necessarily the station's true native sampling rate.
- The Met Office has its own direct API (**Weather DataHub**) — confirmed to exist, confirmed it requires registration + an API key (unlike everything else this project uses), and confirmed it offers something Open-Meteo doesn't: actual map imagery (PNG, temperature/precip/cloud/pressure) plus a "Land Observations API" for ground-station data. **Not confirmed:** that observations API's exact cadence or whether there's a free tier — tried one docs page, it didn't have the answer, and deliberately stopped rather than keep guessing URLs. The forecast/model data itself is confirmed identical to what we already get via Open-Meteo (same UKV), so the only real reason to go direct would be map imagery or fresher observations, not better forecasts.

## Stack

Static HTML/CSS/JS, deployed via GitHub Pages. No build step, no API key, no tracking.

`css/style.css` and `js/app.js` are linked with a `?v=N` query string in `index.html`. Bump `N` whenever either file changes — Safari caches GH Pages assets aggressively and won't otherwise pick up the update.

The footer's "Deployed" timestamp is hand-set in `index.html` on every push (no CI/build step for a repo this size) — update it to the current Europe/London time alongside whatever else changed in that commit.
